{
  "name": "saas-application",
  "version": "1.0.0",
  "description": "Multi-tenant SaaS application",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy": "cd backend && sam deploy --guided",
    "deploy:prod": "cd backend && sam deploy --config-env production"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}