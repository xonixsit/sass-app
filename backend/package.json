{
  "name": "saas-backend",
  "version": "1.0.0",
  "description": "SaaS backend with MySQL database and full CRUD operations",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "dev-old": "node local-server.js",
    "init-db": "node -e \"require('./database').initDatabase().then(() => process.exit(0)).catch(err => { console.error(err); process.exit(1); })\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.16.2",
    "uuid": "^9.0.1"
  }
}
